const registerValidation = require("./auth/registerValidation");
const loginValidation = require("./auth/loginValidation");
const chatValidation = require("./chat/chatValidation");

module.exports = {
  registerValidation,
  loginValidation,
  chatValidation,
};
